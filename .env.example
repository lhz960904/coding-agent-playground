# DeepSeek API key（part 1-3、5 的 OpenAIProvider 默认 baseURL 走 deepseek）
DEEPSEEK_API_KEY=

# Anthropic API key（part 4 演示双 provider 时切到 Claude）
ANTHROPIC_API_KEY=

# 单 IP 每天最多触发多少次 run（防刷，默认 100）
RUN_RATE_LIMIT_PER_DAY=100
